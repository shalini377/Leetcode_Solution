class Solution(object):
    def search(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: int
        """
        l=len(nums)
        val=-1
        for i in range(0,l):
            if nums[i]==target:
                val=i
        return(val)
